%This function Reverse.m flips the array when called by using the flip
%function in matlab
%Inputs = y (1D array)
%Outputs = x (flipped array of y)
%Author: Hong Shi
function [x] = Reverse(y)
x = flip(y);%calls the flip function to reverse the array
end
